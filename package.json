{
  "name": "imagnate-assistant",
  "version": "0.5.0",
  "private": true,
  "description": "Demo assistant for a small electronics store with optional OpenAI-backed answers.",
  "scripts": {
    "start": "node server.js",
    "check": "node -c server.js && node -c assets/js/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
